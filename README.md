# Remote-access-Tool(R.A.T)
A Python remote desktop utility featuring zero-config UDP discovery. It streams real-time screen data and synchronizes keyboard/mouse inputs. The controlled side runs as a silent background service, while the controller offers automatic network scanning or manual IP entry for remote sync.
